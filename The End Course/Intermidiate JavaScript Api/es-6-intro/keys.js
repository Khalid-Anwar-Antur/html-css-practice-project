const glass = {
    name: 'glass',
    color: 'golden',
    price:'12',
    isClean: true
}

const keys = Object.keys(glass)
const value = Object.values(glass)
const pair = Object.entries(glass)
console.log(keys)
console.log(value)
console.log(pair)